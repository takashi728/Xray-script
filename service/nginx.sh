#!/usr/bin/env bash
# nginx.sh — DEPRECATED: Nginx replaced by Caddy (Docker)
# All Nginx operations are handled by handler.sh via Docker
echo "Nginx is no longer used. Use Caddy (Docker image)." >&2
exit 0
